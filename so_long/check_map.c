#include "so_long.h"

int	check_map(char **map)
{
/* 	int portal;
	int player;
	int coin; */

/* 	player;
	coin; */
	if(check_border(map) == 0)
		return (1);
	/* if(check_portal(map) == 0)
		return (1); */
	ft_printf("stefano");
	return 0;
}

