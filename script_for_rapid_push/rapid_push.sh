#!/bin/bash                                                                     

# Move into the specified directory                                             
cd ../lavori42

# Add all changes to the staging area                                           
git add *

# Commit the changes with the message "rapid push"                              
git commit -m "rapid push"

# Push the changes to the remote repository                                     
git push

